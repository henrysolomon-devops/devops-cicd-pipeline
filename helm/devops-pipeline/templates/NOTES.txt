1. Get the application URL:
{{- if contains "LoadBalancer" .Values.service.type }}
  # Since v5.1, this app runs with service.type: LoadBalancer on every
  # environment - k3s's built-in Klipper load balancer exposes it directly
  # on the server's own public IP, on this environment's own port, no
  # port-forward needed.
  echo "Visit http://<server-ip>:{{ .Values.service.port }} to use your application"
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "devops-pipeline.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
{{- else }}
  echo "This chart supports service.type: LoadBalancer or ClusterIP in this project."
{{- end }}
